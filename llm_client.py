"""
Medical Records RAG Demo — LLM Client Wrapper (SRS §5.6)

Single thin wrapper function `chat(messages, system=None) -> str` used by
normalization fallback, graph extraction, PageIndex summarization/traversal,
and answer generation. Provider/model/API key loaded from .env.

Implementation pending — see BUILD_GUIDE Task 2.1.
"""
