"""
Medical Records RAG Demo — Chunking (SRS §5.5)

Chunks report pages into evidence-producing units. Each chunk maps to
exactly one EvidenceRecord. Handles both prose reports (one chunk per page)
and tabular flowsheets (one chunk per row + one whole-table chunk).

Implementation pending — see BUILD_GUIDE Task 1.7.
"""
