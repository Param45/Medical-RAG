"""
Medical Records RAG Demo — Retrieval Orchestration & Answer Generation (SRS §8)

Single entry-point function `answer_question(question, mode, selected_patients,
backend)` that dispatches to GraphRAG or PageIndex retrieval, generates a cited
answer via LLM, and performs a grounding check on citations.

Implementation pending — see BUILD_GUIDE Task 4.1.
"""
