# Medical RAG Demo - Dependencies
# Per SRS §11 and Build Guide Task 0.3

# PDF / Image handling
pypdf>=4.0.0
pdf2image>=1.17.0
Pillow>=10.0.0

# Neo4j driver
neo4j>=5.20.0

# Environment / Config
python-dotenv>=1.0.1

# Streamlit UI
streamlit>=1.35.0

# Testing
pytest>=8.0.0

# LLM SDK (Anthropic Claude or Local Llama-cpp)
anthropic>=0.30.0
llama-cpp-python>=0.3.0

# MinerU OCR (local PDF/OCR pipeline)
# NOTE: magic-pdf[full] installs MinerU with all dependencies including
# layout detection, OCR (PaddleOCR), table recognition, and formula recognition.
# Model weights are downloaded separately via `mineru-models-download`.
# Uncomment or install manually in Task 0.4:
# magic-pdf[full]>=1.0.0

# NOTE: difflib (used for near-duplicate detection and fuzzy matching)
# is part of Python's standard library — no pip install needed.
